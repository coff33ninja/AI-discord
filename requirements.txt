# Core Discord Bot Dependencies
discord.py==2.6.4                      # Discord API wrapper (latest stable)
google-generativeai==0.7.0             # Google Gemini AI engine
python-dotenv==1.0.1                   # Environment variable management

# HTTP & Web Dependencies
aiohttp==3.12.14                       # Async HTTP client
requests==2.32.4                       # HTTP requests library
beautifulsoup4==4.12.3                 # HTML/XML parsing

# Database
aiosqlite==1.3.0                       # Async SQLite database driver

# Web Search
duckduckgo-search==3.9.2               # DuckDuckGo search integration

# Voice & Audio Dependencies
# ⚠️ KittenTTS: Install manually from wheel
# pip install https://github.com/KittenML/KittenTTS/releases/download/0.1/kittentts-0.1.0-py3-none-any.whl

# KittenTTS dependencies (required for voice)
spacy==3.8.7                           # NLP for TTS
num2words==0.5.14                      # Number to words conversion
espeakng-loader==0.2.4                 # eSpeak text-to-phoneme conversion

# Audio Processing
soundfile==0.12.1                      # Audio file I/O
numpy==1.26.4                          # Numerical computing (required by soundfile & Whisper)

# Speech Recognition
openai-whisper==20250623               # OpenAI Whisper speech-to-text (updated)

# Development Dependencies (optional - comment out for production)
# ⚠️ IMPORTANT: watchdog 6.0.0 works with Python 3.10-3.12
# For Python 3.13: skip watchdog or use Python 3.12 instead
watchdog==6.0.0                        # File system monitoring for auto-reload (dev_bot.py)

# Utility packages
python-dateutil==2.8.2                 # Date/time utilities