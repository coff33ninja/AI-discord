# Discord AI Bot Requirements - Python 3.12 Optimized
# 
# RECOMMENDED: Python 3.12.x (all features fully compatible)
# NOT RECOMMENDED: Python 3.13+ (watchdog has threading issues)
#
# For best compatibility, use: Python 3.12.0+

# ===== Core Discord Bot Dependencies =====
# All tested and verified with Python 3.12

py-cord==2.6.1                         # Discord API wrapper with voice recording support (Python 3.8+)
google-generativeai==0.7.0             # Google Gemini 2.5 Flash AI (Python 3.9+)
python-dotenv==1.0.1                   # Environment variable management (Python 3.5+)

# ===== HTTP & Web Dependencies =====
aiohttp==3.12.14                       # Async HTTP client library (Python 3.8+)
requests==2.32.4                       # HTTP requests library (Python 3.7+)
beautifulsoup4==4.12.3                 # HTML/XML parsing for web search (Python 3.6+)

# ===== Database =====
aiosqlite==0.21.0                      # Async SQLite database driver (Python 3.7+)

# ===== Web Search =====
duckduckgo-search==8.1.1            # DuckDuckGo search integration (Python 3.7+)

# ===== Voice & Audio Dependencies =====
# KittenTTS: Ultra-lightweight TTS (~25MB)
# Must install from GitHub release wheel:
# pip install https://github.com/KittenML/KittenTTS/releases/download/0.1/kittentts-0.1.0-py3-none-any.whl

# KittenTTS required dependencies (Python 3.12 compatible)
spacy==3.8.7                           # NLP for TTS (Python 3.8+)
num2words==0.5.14                      # Number to words conversion (Python 3.5+)
espeakng-loader==0.2.4                 # eSpeak text-to-phoneme (Python 3.6+)
PyNaCl==1.5.0                          # Voice encryption for Discord (Python 3.6+)
# Audio processing and computing (Python 3.12 compatible)
soundfile==0.12.1                      # Audio file I/O (Python 3.7+)
numpy==1.26.4                          # Numerical computing (Python 3.9+)

# Speech-to-text (Python 3.12 compatible)
openai-whisper==20250625               # OpenAI Whisper STT (Python 3.9+)

# ===== Development Tools (OPTIONAL) =====
# Comment out if running in production
# 
# ⚠️ IMPORTANT FOR PYTHON 3.12:
# watchdog 6.0.0: Works with Python 3.10-3.12
# DO NOT USE: Python 3.13+ (threading compatibility issues)

watchdog==6.0.0                        # File system monitoring for auto-reload (Python 3.9+)

# ===== Utility Packages =====
python-dateutil==2.8.2                 # Date/time utilities (Python 2.7, 3.3+)


# ===== INSTALLATION NOTES =====
# 
# Using UV Package Manager (RECOMMENDED):
#   uv pip install -r requirements.txt
#   uv pip install --upgrade <package>
# 
# Using PIP:
#   pip install -r requirements.txt
# 
# Installing KittenTTS Wheel:
#   pip install https://github.com/KittenML/KittenTTS/releases/download/0.1/kittentts-0.1.0-py3-none-any.whl
# 
# Voice Recording Setup (REQUIRED for voice features):
#   pip install py-cord[voice]          # Installs py-cord with voice recording support
#   
# Checking Python Version:
#   python --version  (should be 3.12.x)
#
# Verifying Voice Recording Support:
#   python -c "import discord; print('Has sinks:', hasattr(discord, 'sinks'))"
